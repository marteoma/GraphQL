type Query {
  info: String!
  feed: [Link!]!
  link(id: ID!): Link
}

type Mutation {
  postLink(url: String!, description: String!): Link!
}

type Link {
  id: ID!
  description: String!
  url: String!
}